  name: "2.2 workflow"
  on:
    push:
  jobs:
    first_job:
      runs-on: ubuntu-latest
      steps:
          - name: "first jobs"
            run:
              echo " Ceci est le premier job"
    second_job:
      runs-on: ubuntu-latest
      steps:
          - name: "second job"
            run:
              echo " Ceci est le second job"
    third_job:
      runs-on: ubuntu-latest
      steps:
        - name: "third job"
          run:
            echo " Ceci est le troisieme job"